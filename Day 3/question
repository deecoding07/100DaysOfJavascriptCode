//? Write a function call countChar that takes two parameters: a string and a character to count. The function sholud return the number of timees the specified character appears in the string. 

console.log(countChar("Computer Science", "c"));

//*Contraints:
//? The function should be case-sensitive.
//? The function should handle both lowercase and uppercase characters.
//? The character parameter can be any printable ASCII character (the function should accept any character that is part of the ASCII character set and is printable).
