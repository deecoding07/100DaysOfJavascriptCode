//? Write a function arrEqual that takes two arrays arr1 and arr2 as input and returns true if the arrays are equal (i.e, contain the same elements in the same order), and false otherwise.

// Example Usage:
console.log(arrEqual([1, 2, 3], [1, 2, 3])); //Output: true
console.log(arrEqual([1, 2, 3], [1, 2, 4])); //Output: false
console.log(arrEqual([], [])); //Output: true

//* Note:
// The function should return false if the array have different lengths.
// The function should compare each element of arr1 with the corresponding element in arr2.
// The function should return true only if all elements in arr1 are equal to their correspponding elements in arr2.
